// import { mergeRouters } from "@/server/trpc";

// // import { userRouter } from "../modules/user/handler";

// // export const appRouter = mergeRouters(userRouter);

// export type AppRouter = typeof appRouter;
